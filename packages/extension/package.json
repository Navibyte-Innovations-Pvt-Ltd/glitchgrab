{
  "name": "@glitchgrab/extension",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch",
    "test": "bun test src/",
    "zip": "bun run build && bun run zip.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.7",
    "@types/chrome": "^0.0.279",
    "@types/jsdom": "^21.1.7",
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.0.0"
  }
}
